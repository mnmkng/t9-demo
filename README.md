### t9-demo

```
npm install
npm start
```

as usual, in both folders.

Or alternatively.

```
yarn
yarn start
```

The backend is missing a .env file that holds the following keys:
```
MONGO_URI
AUTH_SECRET
CLIENT_ORIGIN
```
